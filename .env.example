# Local Docker Compose / development configuration.
# Copy this file to .env and fill in your own values.
# Never commit real credentials.

# Optional: set only when your network needs a Debian mirror override.
# Example: mirrors.ustc.edu.cn
DEBIAN_MIRROR=

PUBLISHER_AI_BASE_URL=
PUBLISHER_AI_MODEL=
PUBLISHER_AI_API_KEY=
